# 前言

本文主要内容：
- String.fromCodePoint()
- String.raw()
- 实例方法：codePointAt()
- 实例方法：normalize()
- 实例方法：includes(), startsWith(), endsWith()
- 实例方法：repeat()
- 实例方法：padStart()，padEnd()
- 实例方法：trimStart()，trimEnd()
- 实例方法：matchAll()